import { IDate } from "~/interfaces/idate";
/**
 * Datefield represents the mongodb BSON type of Date. It uses the Javascript
 * date type internally for naive representation in the model object.
 *
 * @param name Name of the datafield
 * @param options Configuration options to use to customize behaviour of datafield in model
 */
export declare const Datefield: (name: string, options?: IDate | undefined) => (value?: Date | undefined) => Date | null;
