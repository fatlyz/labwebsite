"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processIndex = function (container, index) {
    if (!index)
        return;
    //todo: Implement try catch here and raise appropriate error i.e. Loader method Not found
    switch (typeof index) {
        case "string":
            container.engine.metadata.indexes.push(container[index]());
            break;
        case "function":
            container.engine.metadata.indexes.push(index());
            break;
        default:
            container.engine.metadata.indexes.push(index);
    }
};
exports.processDefault = function (container, name, def) {
    if (!def)
        return;
    var outcome = typeof def === "function" ? def() : def;
    container.engine.metadata.defaults[name] = outcome;
};
