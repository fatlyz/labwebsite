import { MongoClient, Db } from 'mongodb';
export declare class Connection {
    static db: Db;
    static client: MongoClient;
    static connect(options?: {}): Promise<MongoClient>;
    static database(): Db;
}
