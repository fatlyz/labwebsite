import { Cursor } from 'mongodb';
import { Collection } from "./collection";
interface ICursor {
    next(): Promise<Collection>;
}
export declare class Results<T extends Collection> implements ICursor {
    private cursor;
    private Class;
    constructor(cursor: Cursor, Class: (new () => T));
    getMongoCursor(): Cursor<any>;
    limit(amount: number): Results<T>;
    next(): Promise<T>;
}
export {};
